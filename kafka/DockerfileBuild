FROM golang:1.9

# The kafka example requires librdkafka to be installed.
RUN git clone https://github.com/edenhill/librdkafka.git && \
    cd librdkafka && \
    ./configure --prefix /usr && \
    make && make install

